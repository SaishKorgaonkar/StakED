{
  "name": "staked-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "node": "hardhat node",
    "deploy:localhost": "hardhat run --network localhost scripts/deploy.ts",
    "deploy:sepolia": "hardhat run --network sepolia scripts/deploy.ts",
    "deploy:flow": "hardhat run --network flowTestnet scripts/deploy-flow.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.2",
    "@types/dotenv": "^6.1.1",
    "@types/node": "^24.8.1",
    "dotenv": "^16.6.1",
    "ethers": "^6.15.0",
    "hardhat": "^3.0.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "type": "module",
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
